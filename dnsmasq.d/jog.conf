# DEPRECATED as a hand-edited file for production.
#
# JOG now generates USB-only DHCP/TFTP from /etc/jog/jog.env via:
#   /usr/local/bin/jog-render-dnsmasq.sh -> /etc/dnsmasq.d/99-jog-imaging.conf
#
# dnsmasq is ordered to run that generator before start (see systemd/dnsmasq.service.d/jog-pre.conf).
#
# For a quick lab only, you may still copy snippets from the old jog.conf into 99-jog-imaging.conf,
# but the supported path is: edit /etc/jog/jog.env, then:
#   sudo jog-render-dnsmasq.sh && sudo systemctl restart dnsmasq
